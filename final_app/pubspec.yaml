name: final_app
description: A new Flutter project.

publish_to: 'none' 

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  provider: ^6.0.1
  firebase_core: ^1.7.0
  cloud_firestore: ^2.5.3

dev_dependencies:
  flutter_test:
    sdk: flutter

